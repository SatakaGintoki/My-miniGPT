[project]
name = "miniGPT"
version = "0.1.0"
description = "Mini GPT experiment workspace (self-contained copy of cs336_basics)"
requires-python = ">=3.12,<3.14"
dependencies = [
    "torch~=2.11.0",
    "numpy>=2.4",
    "einops>=0.8",
    "regex",
]
